   {{#each data}}
   <section id="about_section" class="py-5 text-center background2">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h3 class="text-light">{{this.title}}</h3>
                    <p>{{this.para}}</p>
                </div>
            </div>
        </div>
    </section>
    {{/each}}